import { ChatRouteLayout } from "../../components/chat-route-layout";

export default function ChatLayout({ children }: { children: React.ReactNode }) {
  return <ChatRouteLayout>{children}</ChatRouteLayout>;
}
